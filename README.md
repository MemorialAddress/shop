# アプリケーション名

フリマアプリケーション

## 環境構築

- Docker テンプレートのクローン
  $ git clone git@github.com:Estra-Coachtech/laravel-docker-template.git
- Laravel パッケージのインストール
  $ composer install
- DB 接続用環境変数の設定
  $ cp .env.example .env
